/*void main() {
  // Exemplo de uma lista de números inteiros
  List<String> nomes = ['Clara', 'Joao pedro'];
  
  // Acessando elementos da lista
 // print(nome[0]);
  
  // Adicionando um novo elemento à lista
  nomes.add('Rabito');
  
  // Removendo um elemento da lista
  nomes.removeAt(2);
  
  //inserir um elemento especificando posição
  nomes.insert(2, 'João vitor');
  
  //conta para cada indice
  int posicao = 0;
	  nomes.forEach((String nome) {
	  print('$posicao $nome');
	  posicao++;
	  });
  
  // Imprimindo todos os elementos da lista
  print(nomes); // Saída: [1, 2, 4, 5, 6]
}
*/

